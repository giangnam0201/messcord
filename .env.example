# Database (PostgreSQL - use Neon, Supabase, or Vercel Postgres)
DATABASE_URL="postgresql://user:password@host:5432/messcord?sslmode=require"

# NextAuth
NEXTAUTH_SECRET="change-me-to-a-long-random-string"
NEXTAUTH_URL="http://localhost:3000"

# Pusher Channels (for real-time messaging)
# Go to https://dashboard.pusher.com -> Create a "Channels" app (NOT Beams!)
NEXT_PUBLIC_PUSHER_APP_KEY="your-pusher-channels-key"
PUSHER_APP_ID="your-pusher-channels-app-id"
PUSHER_SECRET="your-pusher-channels-secret"
NEXT_PUBLIC_PUSHER_CLUSTER="us2"

# LiveKit (for voice/video/screenshare) - get free keys at https://livekit.io
LIVEKIT_API_KEY="your-livekit-api-key"
LIVEKIT_API_SECRET="your-livekit-api-secret"
NEXT_PUBLIC_LIVEKIT_URL="wss://your-project.livekit.cloud"

# GIF API (GIPHY) - optional, has built-in public beta key
# Get your own at https://developers.giphy.com/
NEXT_PUBLIC_GIPHY_API_KEY=""
