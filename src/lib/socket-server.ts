import type { Server } from 'socket.io';

/**
 * Placeholder for FEAT-003 (real-time chat + voice signaling).
 * Wires Socket.io event handlers to the running io instance.
 */
export function registerSocketHandlers(_io: Server): void {
  // Intentionally empty for FEAT-001. Implemented in FEAT-003.
}
